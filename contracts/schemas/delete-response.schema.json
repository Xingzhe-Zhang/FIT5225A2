{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pacific-bioarchive.local/schemas/delete-response.schema.json",
  "title": "DeleteResponse",
  "type": "object",
  "additionalProperties": false,
  "required": ["results"],
  "properties": {
    "results": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["url", "media_id", "operation_id", "status", "error"],
        "properties": {
          "url": {"type": "string", "format": "uri"},
          "media_id": {"type": ["string", "null"], "format": "uuid"},
          "operation_id": {"type": ["string", "null"], "format": "uuid"},
          "status": {"type": "string", "enum": ["invalid_url", "not_found", "failed", "deleted"]},
          "error": {"type": ["string", "null"]}
        }
      }
    }
  }
}
